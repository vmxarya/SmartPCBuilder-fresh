name = "smart-pc-builder"
type = "javascript"
# Set compatibility_date to a recent date (YYYY-MM-DD)
compatibility_date = "2026-01-01"

# Fill these from your Cloudflare account or use environment substitution in CI
account_id = 742789fa66d4ae51311baaf0ecbc62f2
workers_dev = true

# Optional: if deploying to a specific zone/route, set them:
# zone_id = "YOUR_ZONE_ID"
# route = "example.com/*"

[vars]
# Runtime environment variables (do not store secrets here; prefer CI secrets)
# NEXT_PUBLIC_SUPABASE_URL = ""
# NEXT_PUBLIC_SUPABASE_ANON_KEY = ""
